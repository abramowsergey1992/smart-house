Установка `npm install`
Запуск сборки `gulp`
Отчистить папку и пересобрать `gulp build`
Для сборки в продакшен нужно добавить флаг `--build`
Создать страницу `gulp create --p name --msjpi`
Создать компонент `gulp create --c name --msjpi`

Аргументы для создания страниц модулей (должно идти последним аргументом):
`m` - миксин
`j` - скрипт
`i` - папка с картинками
`t` - шаблон
`s` - стили
